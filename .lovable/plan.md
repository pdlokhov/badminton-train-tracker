

# Исправление парсера недельного расписания

## Проблема

Regex `dayBlockRegex` (строка 234) использует жёстко прописанный список эмодзи в character class. Если в реальном сообщении перед днём недели стоит эмодзи, которого нет в этом списке, блок дня пропускается. Также lookahead использует тот же список, что приводит к тому, что граница между блоками не определяется.

Текущий regex:
```text
(?:[⭐️😀☄️💌🤙🔥❤️...]+\s*)?([А-Яа-я]+)\s*\((\d{1,2}\.\d{1,2})\)...
```

## Решение

Заменить жёстко прописанные эмодзи на универсальный паттерн, который матчит любые символы перед названием дня недели. Вместо попытки перечислить все возможные эмодзи, используем паттерн, который ищет конкретные названия дней недели с датой в скобках.

### Изменения в файле `supabase/functions/parse-channels/index.ts`

**Строка 234** -- заменить `dayBlockRegex` на:

```typescript
const dayNames = '(?:понедельник|вторник|среда|четверг|пятница|суббота|воскресенье)'
const dayBlockRegex = new RegExp(
  `(?:^|\\n)[^\\n]*?(${dayNames})\\s*\\((\\d{1,2}\\.\\d{1,2})\\)([\\s\\S]*?)(?=(?:^|\\n)[^\\n]*?(?:${dayNames})\\s*\\(\\d{1,2}\\.\\d{1,2}\\)|$)`,
  'gim'
)
```

Что это даёт:
- Матчит любой день недели (понедельник-воскресенье), а не произвольное кириллическое слово
- Допускает любые символы (эмодзи, пробелы, спецсимволы) перед названием дня -- `[^\\n]*?`
- Lookahead также использует названия дней, а не эмодзи
- Флаг `m` (multiline) обеспечивает корректную работу `^` для начала строки

### Технические детали

- Один файл: `supabase/functions/parse-channels/index.ts`
- Одно изменение: замена regex на строке 234
- После деплоя функция автоматически обновится

